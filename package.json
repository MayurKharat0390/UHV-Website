{
  "name": "uhv-website",
  "version": "1.0.0",
  "scripts": {
    "vercel-build": "cd uhv_project && python3 -m pip install -r requirements.txt && python3 manage.py migrate --noinput && python3 manage.py collectstatic --noinput --clear && cp -r staticfiles .."
  }
}
